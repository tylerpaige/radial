# Telling Time With Typo

This project started as a display format for text. In its current iteration, it is a clock. Later iterations may choose to use the format for something else. To that end, it has been coded in such a way that the "rays" is a base module and the clock is an implementation of that module. 

The next step of this project to build an interface for me to reflect to on the current hour, on the hour. In this way the project would become a performance.
